# Runtime deps for hosting the live demo backend (Azure App Service).
# The repo's source of truth is pyproject.toml; this mirrors the runtime subset
# so the platform's build step can install without the [dev]/embeddings extras.
duckdb>=1.0
pydantic>=2.0
python-dotenv>=1.0
rich>=13.0
httpx>=0.27
numpy>=1.26
openai>=1.50
# chapter 10's few-shot retrieval reads evals/examples.yaml; without this the
# demo container dies on `import yaml` at startup.
pyyaml>=6.0
